class Node {
    Task task;
    Node next;
    
    // Constructor to create a new node with a given task
    public Node(Task task) {
        this.task = task;
        this.next = null;
    }
}